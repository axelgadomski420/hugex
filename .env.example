# hugex Environment Configuration

# Job execution mode: 'docker' or 'api' (default: 'api')
# EXECUTION_MODE=api

# Docker settings (only needed if using docker mode)
# DOCKER_IMAGE=codex-universal-explore:dev
# DOCKER_MEMORY_LIMIT=536870912
# DOCKER_CPU_SHARES=512
# DOCKER_TIMEOUT=7200000

# Hugging Face API timeout (in seconds, default: 600 = 10 minutes)
# HF_TIMEOUT=600

# Repository settings (can be changed at runtime via UI)
# REPO_URL=https://github.com/drbh/cleanplate
# REPO_BRANCH=main

# OAuth2 Configuration (optional - enables "Sign in with HuggingFace" button)
# If not set, users will need to manually enter their HF token
# OPENID_PROVIDER_URL=https://huggingface.co
# OPENID_CLIENT_ID=your-oauth-app-client-id
# OPENID_CLIENT_SECRET=your-oauth-app-client-secret
# OPENID_CALLBACK_URL=http://localhost:3000/api/auth/callback

# GitHub OAuth2 Configuration (optional - enables private repository access)
# Create OAuth App at: https://github.com/settings/applications/new
# GITHUB_CLIENT_ID=your-github-oauth-app-client-id
# GITHUB_CLIENT_SECRET=your-github-oauth-app-client-secret
# GITHUB_CALLBACK_URL=http://localhost:3000/api/auth/github/callback

# Note: API keys (OPENAI_API_KEY, HUGGINGFACE_TOKEN) are NOT set here
# Users provide these credentials through the web UI at runtime or OAuth2 flow
